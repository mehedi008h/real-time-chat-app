import { Box } from "@chakra-ui/react";
import React, { FC } from "react";

interface IFeedWrapperProps {}

const FeedWrapper: FC<IFeedWrapperProps> = (props) => {
    return <Box>2</Box>;
};

export default FeedWrapper;
